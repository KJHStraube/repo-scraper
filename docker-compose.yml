services:
  readmefetcher:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
    secrets:
      - github_pat
    volumes:
      - repos_data:/data/repos
    restart: unless-stopped

secrets:
  github_pat:
    file: ./secrets/github.pat

volumes:
  repos_data:
